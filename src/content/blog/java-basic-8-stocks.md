---
title: Java基础八股
description: Java后端面试八股整理
pubDate: 2026-03-07T22:28
image: /images/java-basic-8-stocks/35087021d8b10277.png
draft: false
tags:
  - Java
  - 八股
categories:
  - 八股
badge: ''
---
# JDK、JRE、JVM 是什么？
- **JVM**：Java 虚拟机，执行字节码
- **JRE**：Java 运行环境 = JVM + 核心类库
- **JDK**：Java 开发工具包 = JRE + 编译器、调试器
![](/images/java-basic-8-stocks/fa4f6541737360c3.png)

## 在 Java 9 新特性概览这篇文章中，我在介绍模块化系统的时候提到：
> 在引入了模块系统之后，JDK 被重新组织成 94 个模块。Java 应用可以通过新增的 jlink 工具，创建出只包含所依赖的 JDK 模块的自定义运行时镜像。这样可以极大的减少 Java 运行时环境的大小。

# == 和 equals 区别
- **==**：基本类型比值，引用类型比地址
- **equals**：默认比地址，重写后（如 String）比内容

# final、finally、finalize
- **final**：修饰类不能继承、方法不能重写、变量不能改
- **finally**：配合 try-catch，一定执行（除非 JVM 退出）
- **finalize**：Object 方法，垃圾回收前调用，不推荐用

> 2026/3/7

# 什么是字节码？采用字节码的好处是什么？
## 什么是字节码：
Java 字节码（Bytecode） 是 Java 源代码经过编译器（javac）编译后生成的中间二进制文件，它不是操作系统能直接运行的机器码，而是专门给 Java 虚拟机（JVM） 执行的指令集，文件后缀为 .class。
简单理解：
	1. 你写的 .java 源代码 → 编译器编译 → 生成 .class 字节码文件；
	2. 字节码是跨平台的统一指令，不依赖 Windows、Linux、macOS 等具体操作系统；
	3. JVM 会读取字节码，实时翻译成对应操作系统能识别的机器码 并执行
## 采用字节码的好处：
### 1. 实现「一次编写，到处运行」（跨平台性）
这是 Java 最核心的优势。
- 没有字节码时：C/C++ 代码编译后直接生成对应系统的机器码，Windows 编译的程序无法在 Linux 运行，必须重新编译；
- 有字节码时：一份 .class 字节码文件，可以在安装了对应版本 JVM 的任何操作系统上运行，无需修改代码、无需重新编译。
**原理 **：JVM 屏蔽了底层操作系统的差异，字节码只面向 JVM，不面向硬件系统。
### 2. 提升程序执行效率（兼顾解释型与编译型语言优点）
- 解释型语言（如早期 JS）：边解释边执行，速度慢；
- 编译型语言（如 C++）：一次性编译成机器码，速度快，但跨平台差；
- Java 字节码：结合两者优势
	1. 编译阶段：源码编译为紧凑的字节码（比源码解析快很多）；
	2. 运行阶段：JVM 的 JIT 即时编译器 会把高频执行的字节码直接编译成机器码缓存起来，后续直接运行，效率接近原生编译语言。
### 3. 简化虚拟机设计，降低开发成本
字节码是精简、规范的指令集，数量远少于机器码指令。
- 虚拟机开发者无需适配复杂的硬件指令，只需要实现字节码的解析逻辑；
- 让 Java 可以轻松运行在服务器、手机、嵌入式设备等各种硬件平台。
### 4. 支持多语言生态（JVM 多语言兼容）
字节码是 JVM 的通用标准，不只是 Java 能编译成字节码。
### 5. 安全性与可移植性
- 字节码有严格的格式规范，JVM 会对字节码进行校验，防止恶意代码篡改、非法访问系统资源；
- 字节码是二进制紧凑格式，体积小，便于网络传输、分发部署（如 Web 应用、小程序）。

# 为什么说 Java 语言“编译与解释并存”?
Java程序的运行过程
源代码 → 编译（javac）→ 字节码 → 解释执行 + JIT编译（JVM内）→ 本地机器码
这是因为 Java 语言既具有编译型语言的特征，也具有解释型语言的特征。因为 Java 程序要经过先编译，后解释两个步骤，由 Java 编写的程序需要先经过编译步骤，生成字节码（.class 文件），这种字节码必须由 Java 解释器来解释执行
![](/images/java-basic-8-stocks/874a57ebc2997e9f.png)

# AOT有什么优点?为什么不全部使用 AOT呢?
## 一、什么是 AOT？
> **AOT = Ahead-Of-Time 提前编译**
就是在程序运行之前，直接把 Java 代码 / 字节码 一次性编译成 对应操作系统的机器码（.exe/ 可执行文件）。
和 JIT 对比：
- JIT：运行时才编译（边跑边编）
- AOT：运行前就编译好（一次编译，直接运行）
![](/images/java-basic-8-stocks/4357afc7270a665a.png)
## 二、AOT 的优点（非常重要）
1. 启动速度极快（秒起）
不用等 JVM 初始化、不用等 JIT 编译，直接执行机器码。适合：云原生、微服务、Serverless（需要快速启动、快速扩缩容）。
2. 无需预热，第一时间就跑满性能
JIT 需要运行一段时间才能把热点代码编译优化，刚启动很慢（预热）。AOT 一开始就是最优机器码，无预热、无延迟。
3. 内存占用更低
不需要 JIT 编译器、不需要代码缓存，内存占用大幅减少。适合嵌入式、资源有限的设备。
4. 更安全、难以反编译
直接是机器码，不像 class 字节码那样容易反编译。
5. predictable（稳定可预测）
执行性能稳定，不会因为 JIT 编译而出现波动。
## 三、重点：为什么 Java 不全部使用 AOT？
因为 AOT 有致命缺点，而 JIT 有 AOT 永远比不上的优势！
1. AOT 失去了 跨平台性
AOT 编译后是 特定系统的机器码
Windows 编译 → 只能 Windows 跑
Linux 编译 → 只能 Linux 跑
Java 最核心价值 “一次编写，到处运行” 直接废掉！
2. AOT 无法做运行时优化
JIT 可以在运行时根据实际情况做极致优化：
运行时统计哪些代码最常用
运行时观察变量类型
运行时进行激进优化（如去虚拟化、内联）
AOT 是静态编译，不知道运行时数据，优化能力远不如 JIT！长期运行的服务，JIT 最终性能 > AOT。
3. AOT 编译时间极长
项目越大，AOT 编译越慢。开发阶段如果每次改代码都等 AOT 编译，开发效率会暴跌。
4. 不支持 Java 很多动态特性
Java 大量框架依赖动态能力：
- 反射
- 动态代理
- CGLIB
- 字节码增强
- SPI、插件化
- AOT 静态编译无法处理这些动态特性，要么不支持，要么需要复杂配置。
5. 生成文件体积巨大
AOT 会把所有依赖打包成可执行文件，体积非常大。
## 四、总结
**AOT 优点**：启动快、无需预热、内存小、更安全。
**不全部用 AOT 的原因**：
- 失去跨平台
- 优化能力不如 JIT，长期运行性能更低
- 不支持反射、动态代理等 Java 核心动态特性
- 编译慢、体积大

> 2026/3/8